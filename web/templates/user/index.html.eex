<h1>Listing User</h1>
<table class="table">
  <%= for user <- @users do %>
  <tr>
    <td><strong><%= first_name(user) %></strong> (<%= user.id %>)</td>
    <td><%= link "View", to: user_path(@conn, :show, user.id) %></td>
  </tr>
  <% end %>
</table>
